nav#nav
  span#blog_name
    a#site-name(href=url_for('/')) #[=config.title]
    
  #menus
    !=partial('includes/header/menu_item', {}, {cache: true})

    
  #right-icons
    if (theme.algolia_search.enable || theme.local_search.enable)
      #search-button
        div.cursor.social-icon.search
          i.fas.fa-search.fa-fw
        //-   span=' '+_p('search.title')
    div#darkmodeCopy(type="button" title=_p('rightside.night_mode_title')).cursor
            i.fas.fa-adjust
    #toggle-menu
      a.site-page
        i.fas.fa-bars.fa-fw
    


